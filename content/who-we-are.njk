---
title: Who we are
description: "Our Community Safety Managers are the main point of contact for communities, schools and partners across the country."
kicker: About Us
lastUpdated: 2026-03-26
layout: layouts/page-full.njk
eleventyNavigation:
  key: Who we are
  order: 2
useMap: true
---

{# ---- Who we are content ---- #}


{% from "components/feature/macro.njk" import feature %}
{% from "components/button/macro.njk" import button %}
{% from "components/tag/macro.njk" import tag %}
{% from "components/sectionHeader/macro.njk" import sectionHeader %}


{# ---- What is a CSM? ---- #}

<section class="o-section">
  <div class="o-container o-container--lg">
    {{ sectionHeader({
      kicker: title,
      title: "What is a Community Safety Manager?"
    }) }}
    {# description: "Our Community Safety Managers deliver practical support and coordinated safety initiatives at the heart of local communities." #}
    <div class="o-prose">
        <p>A Community Safety Manager works closely with local communities, individuals, partners and organisations to understand safety concerns, coordinate engagement activities and support collaborative solutions.</p>
        <p>Their role is to support communities, partners and organisations across the rail network with any railway safety concerns, engagement requests or partnership opportunities.</p>
        <h3>How can your local CSM help you?</h3>
        <ul>
          <li>School or youth engagement requests</li>
          <li>Local safety concerns (trespass, ASB, community impact)</li>
          <li>Partnership working with councils, BTP, charities or community groups</li>
          <li>Support with campaigns, events or outreach initiatives</li>
          <li>Guidance on rail safety messaging, resources and materials</li>
        </ul>
        <div class="o-stack">
          {{ button({ label: "Get in touch", url: "contact.njk", style: "primary" }) }}
          {{ button({ label: "Find your local Community Safety Manager", url: "contact.njk#csm-finder-heading", style: "outline" }) }}
        </div>
    </div>
  </div>
</section>

{# ---- How We Work ---- #}

<section class="o-section u-bg-cream">
  <div class="o-container">
    {{ sectionHeader({
      kicker: "Our Approach",
      title: "How We Work",
      isCentered: true
    }) }}
    <div class="o-grid o-grid--3">
      {{ feature({
        icon: "👂",
        title: "Listen",
        text: "We start by understanding the concerns and priorities of the communities we serve."
      }) }}
      {{ feature({
        icon: "🤝",
        title: "Collaborate",
        text: "We bring together partners — schools, police, councils, charities — to develop joined-up solutions."
      }) }}
      {{ feature({
        icon: "📈",
        title: "Deliver",
        text: "We implement targeted initiatives and measure their impact to continuously improve safety outcomes."
      }) }}
    </div>

  </div>
</section>

{# ---- Key Partners ---- #}

<section class="o-section">
  <div class="o-container o-container--lg">
    {{ sectionHeader({
      kicker: "Working Together",
      title: "Key Partners",
      description: "We work in partnership with a range of organisations to keep communities safe around the railway."
    }) }}
    <div class="o-cluster o-cluster--lg">
      {{ tag({ text: "British Transport Police", type: "primary" }) }}
      {{ tag({ text: "Network Rail", type: "primary" }) }}
      {{ tag({ text: "Local Councils", type: "primary" }) }}
      {{ tag({ text: "Schools & Colleges", type: "primary" }) }}
      {{ tag({ text: "Youth Services", type: "primary" }) }}
      {{ tag({ text: "Community Groups", type: "primary" }) }}
      {{ tag({ text: "Charities", type: "primary" }) }}
      {{ tag({ text: "Train Operating Companies", type: "primary" }) }} 
    </div>
  </div>
</section>


{# ---- CSM Finder ---- #}

{% include "partials/csm-finder.njk" %}
